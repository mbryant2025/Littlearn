# LittleLearn Web Interface

## Running

First, be sure to install the node packages. To do so, run the following commands from within the `littlelearn-web` directory.

```bash
npm install
```

Then, to run locally, run:

```bash
npm start
```